from datetime import datetime


current_time = datetime.now().time()
if current_time.hour < 12:
        greeting = "Good Morning"
elif current_time.hour < 18:
        greeting = "Good Afternoon"
else:
        greeting = "Good Night"
print(f"{greeting}, it's {current_time}")

